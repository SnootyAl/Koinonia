<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Koinonia.Views.ContactInfoPage">
    <ContentPage.Content>
        
        <StackLayout>

            <Entry x:Name="First" Placeholder="First" Text="{Binding CurrentContact.FirstName}" ReturnType="Next" IsReadOnly="{Binding EditDisabled}"/>
            <Entry x:Name="Last" Placeholder="Last" Text="{Binding CurrentContact.LastName}" IsReadOnly="{Binding EditDisabled}"/>
            <Entry x:Name="Phone" Placeholder="Phone" Keyboard="Numeric" Text="{Binding CurrentContact.PhoneNumber}" IsReadOnly="{Binding EditDisabled}"/>
            <Entry x:Name="Email" Placeholder="Email" Text="{Binding CurrentContact.Email}" IsReadOnly="{Binding EditDisabled}"/>

            <StackLayout Orientation="Horizontal">
                <!--Button Text="Cancel" Command="{Binding CancelCommand}"/-->
                <Button Text="{Binding EditButtonText}" Command="{Binding EditCommand}"/>
            </StackLayout>

        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>