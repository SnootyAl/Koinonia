<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Koinonia.Views.ContactInfoPage">
    <ContentPage.Content>
        
        <StackLayout>

            <Entry Placeholder="First" Text="{Binding Contact.FirstName}" ReturnType="Next" IsReadOnly="{Binding EditDisabled}"/>
            <Entry Placeholder="Last" Text="{Binding Contact.LastName}" IsReadOnly="{Binding EditDisabled}"/>
            <Entry Placeholder="Phone" Text="{Binding Contact.PhoneNumber}" IsReadOnly="{Binding EditDisabled}" Keyboard="Numeric" />
            <Entry Placeholder="Email" Text="{Binding Contact.Email}" IsReadOnly="{Binding EditDisabled}"/>
            <Entry Placeholder="Notes" Text="{Binding Contact.Notes}" IsReadOnly="{Binding EditDisabled}"/>

            <StackLayout Orientation="Horizontal">
                <!--Button Text="Cancel" Command="{Binding CancelCommand}"/-->
                <Button Text="{Binding EditButtonText}" Command="{Binding EditCommand}"/>
            </StackLayout>

        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>