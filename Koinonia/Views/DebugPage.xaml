<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xampane="clr-namespace:XamPane;assembly=XamPane"
             xmlns:polygon="clr-namespace:Xamarin.Forms.PolygonFrame;assembly=Xamarin.Forms.PolygonFrame"
             mc:Ignorable="d"
             x:Class="Koinonia.Views.DebugPage">
    <ContentPage.Content>
        <ScrollView Orientation="Both">
            <Grid x:Name="ContainerGrid" BackgroundColor = "#17A7B2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="{Binding screenHeight}" />
                    
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{Binding screenWidth}" />
                  
                </Grid.ColumnDefinitions>
                
                
            </Grid>
        </ScrollView>
    </ContentPage.Content>
   
</ContentPage>